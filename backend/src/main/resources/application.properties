# Server Configuration
server.port=${PORT:8080}

# MySQL Database Configuration (lokalt via Docker)
spring.datasource.url=jdbc:mysql://localhost:3306/portfolio_db
spring.datasource.username=portfolio_user
spring.datasource.password=password123

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# RabbitMQ Configuration
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

# Actuator Configuration
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Logging
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=WARN
logging.level.org.springframework.amqp=INFO

# MailerSend Email Configuration
mailersend.api.key=${MAILERSEND_API_KEY}
mailersend.from.email=${MAILERSEND_FROM_EMAIL:noreply@gustavnyberg.se}
mailersend.from.name=${MAILERSEND_FROM_NAME:Gustav Nyberg Portfolio}
mailersend.to.email=${MAILERSEND_TO_EMAIL:gustavnybergs@outlook.com}
